.DS_Store
*.swp
.idea/
.vscode/
node_modules/
.vite/
.env
.env.local
*.log
# tsc -b artifacts (generated, not source)
*.tsbuildinfo
vite.config.js
vite.config.d.ts
vite.config.js.timestamp-*
# Vite build temp dir (the npm script copies its output to root /assets + /index.html)
.build/
# /docs is no longer used — kept around in /docs only briefly, see docs/.nojekyll
# /assets and /index.html ARE committed (built output served by GH Pages from main / root)
